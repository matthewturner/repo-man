# Repo Manager

Shows the list of repos matching the search criteria which have changed over a period of time.

## Installation

1. Copy `settings.sample.json` to `settings.json`
1. Install Github CLI from [here](https://cli.github.com/manual/installation)
2. Login to Github with `gh auth login`
3. Run `./repo-man.ps1`